{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd .. && npm install && npm run build --workspace=server && cd server && npx prisma generate"
  },
  "deploy": {
    "startCommand": "node dist/api.js"
  }
}
